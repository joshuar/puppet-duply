class duply::params {
  $ensure = 'present'
  # This seems pretty useless, but we are following
  # best practice for module creation and don't know
  # if there will be other parameters required in the
  # future.
}
